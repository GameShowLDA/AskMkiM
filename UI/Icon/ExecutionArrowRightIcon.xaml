<UserControl x:Class="UI.Icon.ExecutionArrowRightIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root"
             Width="{Binding Size, ElementName=root}"
             Height="{Binding Size, ElementName=root}">
    <UserControl.Resources>
        <StreamGeometry x:Key="SemiIconFilledArrowUp">
            m1.85 11.15 9.8-9.8c.2-.2.5-.2.7 0l9.8 9.8a.5.5 0 0 1-.36.85H16v9a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1v-9H2.2a.5.5 0 0 1-.35-.85Z
        </StreamGeometry>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Canvas Width="22" Height="22">
            <Path Data="{StaticResource SemiIconFilledArrowUp}"
                  Fill="{Binding FillBrush, ElementName=root}"
                  RenderTransformOrigin="0.5,0.5">
                <Path.RenderTransform>
                    <RotateTransform Angle="90"/>
                </Path.RenderTransform>
            </Path>
        </Canvas>
    </Viewbox>
</UserControl>
