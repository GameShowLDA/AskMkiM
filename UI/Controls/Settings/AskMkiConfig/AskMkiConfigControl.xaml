<UserControl x:Class="UI.Controls.Settings.AskMkiConfig.AskMkiConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Icons="clr-namespace:UI.Icon"
             mc:Ignorable="d"
             d:DesignHeight="180"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="AskMkiPathTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Foreground" Value="{DynamicResource SettingsTitleSolidColorBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SettingsInteractiveBackgroundSolidColorBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="AskMkiConfigManager"
            Style="{DynamicResource SettingPanelStyle}"
            Margin="15,10">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Source={StaticResource Settings_AskMkiConfig_Title}, Path=Value}"
                           Padding="10,10"
                           Style="{DynamicResource SettingsTitleStyle}"
                           VerticalAlignment="Center"
                           Grid.Column="0"/>

                <Grid Grid.Column="1"
                      Margin="20,0">

                    <Icons:ArrowButton x:Name="ToggleAskMkiConfigButton"
                                       HorizontalAlignment="Right"
                                       Width="30"
                                       Height="30"
                                       ButtonBackground="{DynamicResource SettingsInteractiveBackgroundSolidColorBrush}"
                                       ButtonBorder="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"
                                       ArrowBrush="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"
                                       ArrowHoverBrush="{DynamicResource SettingsInteractivePressedSolidColorBrush}"
                                       IsArrowUp="True"
                                       VerticalAlignment="Center"/>
                </Grid>
            </Grid>

            <ContentControl Grid.Row="1">
                <ContentControl.Style>
                    <Style TargetType="{x:Type ContentControl}">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=ToggleAskMkiConfigButton, Path=IsArrowUp}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>

                <Border Style="{DynamicResource SettingPanelStyle}"
                        Margin="10,8,10,12"
                        Padding="14">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="{Binding Source={StaticResource Settings_AskMkiConfig_MkiPath_Title}, Path=Value}"
                                   Style="{DynamicResource SettingsTitleStyle}"
                                   FontWeight="SemiBold"/>

                        <TextBlock Grid.Row="1"
                                   Text="{Binding Source={StaticResource Settings_AskMkiConfig_MkiPath_Description}, Path=Value}"
                                   TextWrapping="Wrap"
                                   Margin="0,6,0,0"
                                   Opacity="0.8"
                                   Style="{DynamicResource SettingsTitleStyle}"/>

                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="MkiPathTextBox"
                                     Grid.Column="0"
                                     Style="{StaticResource AskMkiPathTextBoxStyle}"/>

                            <Button Grid.Column="2"
                                    Content="{Binding Source={StaticResource Settings_AskMkiConfig_SelectMki}, Path=Value}"
                                    Style="{StaticResource DeviceInputSecondaryActionButton}"
                                    MinWidth="140"
                                    Click="SelectMkiPathButton_Click"/>

                            <Button x:Name="ClearMkiPathButton"
                                            Grid.Column="4"
                                            Content="{Binding Source={StaticResource Settings_AskMkiConfig_ClearMki}, Path=Value}"
                                            Style="{StaticResource DeviceInputSecondaryActionButton}"
                                            MinWidth="140"
                                            Click="ClearMkiPathButton_Click"/>
                        </Grid>
                    </Grid>
                </Border>
            </ContentControl>
        </Grid>
    </Border>
</UserControl>