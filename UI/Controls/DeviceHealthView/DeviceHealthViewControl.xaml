<UserControl x:Class="UI.Controls.DeviceHealthView.DeviceHealthViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Controls.DeviceHealthView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource ErrorListBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <ItemsControl x:Name="DevicesList">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding NameNumber}"
                            Margin="5"
                            Padding="10"
                            PreviewMouseDown="DevicePreviewMouseDown"
                            Style="{DynamicResource ButtonStyleV8}"
                            Tag="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Background="{DynamicResource BackgroundBrushes}" CornerRadius="10" Grid.Column="1" Margin="5">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="0"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="{DynamicResource SettingsMainBackground}" Padding="10" CornerRadius="10" Margin="0,0,0,5">
                    <TextBlock Name="Header" Text="Выберите устройство для просмотра его состояния" FontSize="20" Foreground="{DynamicResource ForegrounfBrushes}" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Margin="10" Grid.Row="1" 
                   Foreground="{DynamicResource ForegrounfBrushes}" 
                   VerticalAlignment="Top" 
                   HorizontalAlignment="Left" 
                   FontSize="16"
                   TextWrapping="Wrap"
                   Name="Desription"
                   />
            </Grid>
            
        </Border>
    </Grid>
</UserControl>
