<Window x:Class="UI.Controls.Calendar.CalendarNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Components="clr-namespace:UI.Components"
        mc:Ignorable="d"
        Title="Заметка"
        Width="560"
        Height="360"
        MinWidth="500"
        MinHeight="320"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{DynamicResource ForegroundSolidColorBrush}"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="Components:WindowButtons">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSolidColorBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundSolidColorBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DialogSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="{DynamicResource WinstonMedium}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSolidColorBrush}"/>
            <Setter Property="Background" Value="{DynamicResource IsCheckedColorSolidColorBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ForegroundSolidColorBrush60}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ActiveBorderSolidColorBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2A3140"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1F2430"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSolidColorBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundSolidColorBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="{DynamicResource WinstonMedium}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource SecondarySolidColorBrush}"
            BorderBrush="{DynamicResource SettingsPanelBorderSolidColorBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30"
                              ShadowDepth="0"
                              Opacity="0.32"
                              Color="#C0000000"/>
        </Border.Effect>

        <DockPanel>
            <Border DockPanel.Dock="Top"
                    Height="46"
                    Background="{DynamicResource IsCheckedColorSolidColorBrush}"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Заметка"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource WinstonMedium}"
                               FontSize="18"
                               FontWeight="SemiBold"/>

                    <Components:WindowButtons HorizontalAlignment="Right"
                                              Width="44"
                                              Height="44"
                                              ChoiceElement="Exit"
                                              Command="{x:Static SystemCommands.CloseWindowCommand}"/>
                </Grid>
            </Border>

            <Grid Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource IsCheckedColorSolidColorBrush}"
                        CornerRadius="12"
                        Margin="0,0,0,18"
                        Padding="14">
                    <TextBox x:Name="noteTextBox"
                             Style="{StaticResource DialogTextBoxStyle}"/>
                </Border>

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="Отмена"
                            Style="{StaticResource DialogSecondaryButtonStyle}"
                            IsCancel="True"/>
                    <Button Content="Сохранить"
                            Width="170"
                            Height="44"
                            Margin="12,0,0,0"
                            Click="SaveButton_Click"
                            Style="{DynamicResource ButtonStyleV10}"/>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Border>
</Window>
