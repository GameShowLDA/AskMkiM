<UserControl x:Class="UI.Components.InputFieldBreakdown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components"
             mc:Ignorable="d" d:DesignWidth="200" Height="390">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:TextBoxPlaceholder x:Name="FirstTextBox" Margin="5" Grid.Row="0" Placeholder="Первая точка вида a.b.c"/>
        <local:TextBoxPlaceholder x:Name="SecondTextBox" Margin="5" Grid.Row="1" Placeholder="Вторая точка вида a.b.c"/>
        <local:TextBoxPlaceholder x:Name="ElectricalTextBox" Margin="5" Grid.Row="2" Placeholder="Электрический параметр"/>
        <local:TextBoxPlaceholder x:Name="TimeTextBox" Margin="5" Grid.Row="3" Placeholder="Время выполнения в сек."/>
        <local:TextBoxPlaceholder x:Name="VoltageTextBox" Margin="5" Grid.Row="4" Placeholder="Напряжение в В."/>

    </Grid>
</UserControl>