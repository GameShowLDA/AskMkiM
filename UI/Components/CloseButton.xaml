<UserControl x:Class="UI.Components.CloseButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Components"
             mc:Ignorable="d"
             d:DesignHeight="20"
             d:DesignWidth="20"
             MinWidth="20"
             MaxWidth="20"
             MinHeight="20"
             MaxHeight="20">

    <Border x:Name="BackgroundBorder"
            Margin="2"
            Background="Transparent"
            CornerRadius="10">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource TopPanelTabCloseButtonHoverBackgroundBrush}"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Rectangle Width="15"
                       Height="2"
                       RadiusX="1.5"
                       RadiusY="1.5"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       RenderTransformOrigin="0.5,0.5">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonForegroundBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=BackgroundBorder}" Value="True">
                                <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonHoverForegroundBrush}"/>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonDisabledForegroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="45"/>
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle Width="15"
                       Height="2"
                       RadiusX="1.5"
                       RadiusY="1.5"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       RenderTransformOrigin="0.5,0.5">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonForegroundBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=BackgroundBorder}" Value="True">
                                <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonHoverForegroundBrush}"/>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Fill" Value="{DynamicResource TopPanelTabCloseButtonDisabledForegroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="-45"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </Grid>
    </Border>

</UserControl>
