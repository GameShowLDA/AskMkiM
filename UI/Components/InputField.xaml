<UserControl x:Class="UI.Components.InputField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:TextBoxPlaceholder Margin="5" Grid.Row="0" Placeholder="Первая точка вида a.b.c"/>
        <local:TextBoxPlaceholder Margin="5" Grid.Row="1" Placeholder="Вторая точка вида a.b.c"/>
        <local:TextBoxPlaceholder Margin="5" Grid.Row="2" Placeholder="Первая точка вида a.b.c"/>
    </Grid>
</UserControl>
