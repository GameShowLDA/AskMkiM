<Window x:Class="UI.Components.SearchControls.SearchResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Components.SearchControls"
        mc:Ignorable="d"
        Title="Результаты поиска" Height="450" Width="600"
        ShowInTaskbar="False"
        Background="{StaticResource SecondarySolidColorBrush}"
        WindowStartupLocation="CenterScreen"
        >
    <Grid>
        <DataGrid x:Name="ResultsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  ClipboardCopyMode="None"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  Background="Transparent"
                  PreviewMouseDoubleClick="ResultsDataGrid_PreviewMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Документ" Binding="{Binding FileName}" Width="200"/>
                <DataGridTextColumn Header="Строка" Binding="{Binding LineNumber}" Width="100"/>
                <DataGridTextColumn Header="Текст" Binding="{Binding LineText}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
