<UserControl x:Class="UI.Components.ProtocolListBox.ProtocolListBoxUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Components.ProtocolListBox"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </UserControl.Resources>

    <Grid>
        <Border CornerRadius="20" Background="{StaticResource SecondarySolidColorBrush}">
            <ListBox x:Name="ProtocolListBox"
                 Background="Transparent"
                 BorderThickness="0"
                 ItemsSource="{Binding Messages, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    VirtualizingStackPanel.IsVirtualizing="True"
                    VirtualizingStackPanel.VirtualizationMode="Recycling"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    SelectionMode="Single"
                    Focusable="True"
                    IsHitTestVisible="True"
                 >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="Wrap" FontSize="20">
                        <Run Text="{Binding Header}" Foreground="{Binding HeaderColor, Converter={StaticResource ColorToBrushConverter}}" />
                        <Run Text=": " Foreground="{Binding HeaderColor, Converter={StaticResource ColorToBrushConverter}}"/>
                        <Run Text="{Binding Message}" Foreground="{Binding MessageColor, Converter={StaticResource ColorToBrushConverter}}" />
                        <Run Text="{Binding Time}" Foreground="YellowGreen" />
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="IsHitTestVisible" Value="False" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="FontFamily" Value="Consolas" />
                    </Style>
                </ListBox.ItemContainerStyle>

            </ListBox>
        </Border>
    </Grid>
</UserControl>
