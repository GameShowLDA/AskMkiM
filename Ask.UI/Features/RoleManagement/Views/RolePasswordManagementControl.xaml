<UserControl x:Class="Ask.UI.Features.RoleManagement.Views.RolePasswordManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Icons="clr-namespace:Ask.UI.Shared.Components.Icons"
             mc:Ignorable="d"
             d:DesignHeight="280"
             d:DesignWidth="760">
    <UserControl.Resources>
        <Style x:Key="RolePasswordInputStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegrounfBrushes}"/>
            <Setter Property="Background" Value="{DynamicResource SettingsInteractiveBackgroundSolidColorBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegrounfBrushes}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <Grid Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="Center"
                                  Height="24">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource SettingsInteractiveHoverSolidColorBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource SettingsInteractiveActiveSolidColorBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RolePasswordSaveButtonStyle" TargetType="Button" BasedOn="{StaticResource SaveButtonStyle}">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="{DynamicResource WinstonMedium}"/>
        </Style>
    </UserControl.Resources>

    <Border Width="760"
            MaxWidth="760"
            Margin="15,10"
            HorizontalAlignment="Left"
            Padding="0"
            Style="{DynamicResource SettingPanelStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="18,14,18,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Смена пароля"
                               Style="{DynamicResource SettingsTitleStyle}"
                               FontSize="22"
                               Margin="0,0,0,6"/>
                    <TextBlock Text="Выберите роль и задайте новый пароль без ввода текущего пароля."
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               FontSize="13"/>
                </StackPanel>

                <Icons:ArrowButton x:Name="TogglePasswordSettingsButton"
                                   Grid.Column="1"
                                   Width="30"
                                   Height="30"
                                   ButtonBackground="{DynamicResource SettingsInteractiveBackgroundSolidColorBrush}"
                                   ButtonBorder="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"
                                   ArrowBrush="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"
                                   ArrowHoverBrush="{DynamicResource SettingsInteractivePressedSolidColorBrush}"
                                   VerticalAlignment="Center"/>
            </Grid>

            <Grid Grid.Row="1"
                  Margin="18,2,18,16">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=TogglePasswordSettingsButton, Path=IsArrowUp}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="210"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Роль"
                               Style="{DynamicResource SettingsHeaderStyle}"
                               FontSize="14"/>
                    <TextBlock Text="Выберите, чей пароль изменить"
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               Margin="0,5,0,0"/>
                </StackPanel>
                <ComboBox x:Name="RoleComboBox"
                          Grid.Row="0"
                          Grid.Column="2"
                          Height="38"
                          ItemsSource="{Binding Roles}"
                          SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          IsEnabled="{Binding IsNotBusy}"
                          Style="{StaticResource CustomComboBoxStyle}"
                          SelectionChanged="RoleComboBox_SelectionChanged"/>

                <StackPanel Grid.Row="2" Grid.Column="0">
                    <TextBlock Text="Новый пароль"
                               Style="{DynamicResource SettingsHeaderStyle}"
                               FontSize="14"/>
                    <TextBlock Text="Введите новый пароль"
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               Margin="0,5,0,0"/>
                </StackPanel>
                <PasswordBox x:Name="NewPasswordBox"
                             Grid.Row="2"
                             Grid.Column="2"
                             Style="{StaticResource RolePasswordInputStyle}"
                             IsEnabled="{Binding IsNotBusy}"
                             PasswordChanged="NewPasswordBox_PasswordChanged"/>

                <StackPanel Grid.Row="4" Grid.Column="0">
                    <TextBlock Text="Подтвердите пароль"
                               Style="{DynamicResource SettingsHeaderStyle}"
                               FontSize="14"/>
                    <TextBlock Text="Повторите новый пароль"
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               Margin="0,5,0,0"/>
                </StackPanel>
                <PasswordBox x:Name="ConfirmPasswordBox"
                             Grid.Row="4"
                             Grid.Column="2"
                             Style="{StaticResource RolePasswordInputStyle}"
                             IsEnabled="{Binding IsNotBusy}"
                             PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
            </Grid>

            <Border Grid.Row="2"
                    Padding="18,14"
                    BorderBrush="{DynamicResource SettingsInteractiveBorderSolidColorBrush}"
                    BorderThickness="0,1,0,0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=TogglePasswordSettingsButton, Path=IsArrowUp}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding StatusMessage}"
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               Foreground="{DynamicResource ForegrounfBrushes45}"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>

                    <Button Grid.Column="1"
                            Style="{StaticResource RolePasswordSaveButtonStyle}"
                            Content="Сохранить пароль"
                            Command="{Binding SavePasswordCommand}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
